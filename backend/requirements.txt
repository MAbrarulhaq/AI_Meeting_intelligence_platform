# ==========================
# FastAPI
# ==========================
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv==1.0.1

# ==========================
# Whisper
# ==========================
openai-whisper==20250625

# ==========================
# PyTorch
# Pinned as a matched trio — do NOT bump any one of these
# independently, that's what breaks pyannote.
# ==========================
torch==2.1.0
torchaudio==2.1.0
numpy==1.26.4

# ==========================
# Speaker Diarization
# huggingface_hub and speechbrain are pinned explicitly (not left
# to pip's resolver) because newer releases of both break pyannote
# 3.1.1's internal calls (use_auth_token / cached_download removal,
# speechbrain.pretrained -> speechbrain.inference rename).
# ==========================
pyannote.audio==3.1.1
huggingface_hub==0.20.3
speechbrain==0.5.16

# ==========================
# Audio
# ==========================
soundfile==0.12.1

# AI Meeting Intelligence (Phase 4)
# langchain-google-genai 4.x uses the newer 'google-genai' SDK
# internally (not the older 'google-generativeai'). langchain-core is
# pulled in standalone (not the full 'langchain' meta-package) since
# only PromptTemplate/LCEL are needed here - smaller footprint, fewer
# places for version conflicts to creep in.
# ==========================
langchain-core==1.5.3
langchain-google-genai==4.2.5


# ==========================
# Persistence (Phase 5)
# psycopg (v3) preferred over psycopg2 per spec. [binary] avoids
# needing PostgreSQL build tools/headers installed locally.
# ==========================
sqlalchemy==2.0.51
alembic==1.18.5
psycopg[binary]==3.2.9


# ==========================
# Authentication (Phase 6)
# bcrypt is called directly, NOT via passlib — passlib is unmaintained
# and incompatible with every bcrypt release >=4.0 (removed the
# __about__.__version__ attribute passlib's version detection reads).
# See app/security/password.py for the full explanation.
# email-validator is required for Pydantic's EmailStr type.
# ==========================
bcrypt==4.2.0
python-jose[cryptography]==3.3.0
email-validator==2.2.0


# ==========================
# RAG Chatbot (Phase 7)
# Local persistent Chroma store - no separate server process needed.
# ==========================
chromadb==1.5.9